<h1>Hi %recipient.firstName%</h1>
<p class="text-larger">An enquiry was just submitted to {{brand}}:</p>
{{#if enquiry.email}}
	<p class="text-larger">From
	{{#if enquiry.name.full}}
		<strong>{{enquiry.name.full}}</strong>
	{{/if}}

	{{#if enquiry.email}}
		(<a href="mailto:{{enquiry.email}}">{{enquiry.email}}</a>)
	{{/if}}
	</p>

	{{#if enquiry.phone}}
		<p class="text-larger">Phone: {{enquiry.phone}}</p>
	{{/if}}

	<!-- {{#if enquiry.enquiryType}}
		<p class="text-larger">{{enquiry.enquiryType}}</p>
	{{/if}} -->

	{{#if enquiry.message}}
	<p class="text-larger">Message:</p>
		{{{enquiry.message}}}
	{{/if}}

	<p class="text-muted">Sent {{underscoreFormat enquiry 'createdAt'}}</p>
{{/if}}
